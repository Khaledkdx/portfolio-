import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function GulfProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.courtyard} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>KHALID</Link><span>PROJECT COURTYARD · 0{d.index+1}</span></nav><header><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.arch}/>:null}</header><section className={s.gates}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><i>0{i+1}</i><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.walk}>{d.images.slice(1).map((image,i)=><ProjectPicture key={image.id} image={image} locale={locale} className={i%2?s.low:s.high}/>)}</section>:null}<section className={s.majlis}><p>{project.tools.join(" · ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}</section><footer><Link href={`${d.base}${d.query}`}>← COURTYARD</Link>{d.next?<Link href={d.projectUrl(d.next.slug)}>NEXT GATE →</Link>:null}</footer></main>}
