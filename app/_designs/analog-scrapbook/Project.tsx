import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function ScrapProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.desk} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>← SCRAPBOOK</Link><span>PAGE {d.index+1}</span></nav><header><div className={s.note}><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.polaroid}/>:null}</header><section className={s.clips}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><b>{pick(l,locale)}</b><p>{pick(t,locale)}</p><i>0{i+1}</i></article>)}</section>{d.images.length>1?<section className={s.album}>{d.images.slice(1).map((image,i)=><ProjectPicture key={image.id} image={image} locale={locale} className={i%2?s.right:s.left}/>)}</section>:null}<section className={s.tape}><p>{project.tools.join(" + ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}</section></main>}
