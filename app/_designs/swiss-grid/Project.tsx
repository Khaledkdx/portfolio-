import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function SwissProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.grid} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>K / INDEX</Link><b>0{d.index+1}—PROJECT</b></nav><header><span>{String(d.index+1).padStart(2,"0")}</span><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div></header>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.image}/>:null}<section className={s.facts}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><b>0{i+1}</b><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.gallery}>{d.images.slice(1).map(image=><ProjectPicture key={image.id} image={image} locale={locale}/>)}</section>:null}<footer><p>{project.tools.join(" / ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}</footer></main>}
