import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function RetroProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.crt} dir={d.dir}><div className={s.bar}><Link href={`${d.base}${d.query}`}>▣ KHALID_OS</Link><span>12:45 PM</span></div><section className={s.desktop}><header><div className={s.title}>PROJECT_{d.index+1}.EXE <b>_ □ ×</b></div><div className={s.window}><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.preview}/>:null}</div></header><section className={s.files}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><div>NOTE_0{i+1}.TXT</div><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.photos}>{d.images.slice(1).map((image,i)=><div key={image.id}><label>IMG_{String(i+1).padStart(3,"0")}.BMP</label><ProjectPicture image={image} locale={locale}/></div>)}</section>:null}<section className={s.cmd}>C:\PROJECT&gt; {project.tools.join(" + ")}<br/>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">START {pick(l.label,locale)} ↗</a>)}</section></section></main>}
