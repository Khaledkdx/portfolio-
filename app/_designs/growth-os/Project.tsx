import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function OsProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);const nodes=[{code:"INPUT",label:content.labels.challenge,text:project.challenge},{code:"ENGINE",label:content.labels.solution,text:project.solution},{code:"OUTPUT",label:content.labels.outcome,text:project.outcome}];return <main className={s.os} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>◉ GROWTH.OS</Link><span>PROJECT/{String(d.index+1).padStart(2,"0")}</span></nav><header><div><i>CASE_MODULE.RUNNING</i><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.screen}/>:null}</header><section className={s.pipeline}>{nodes.map((node,i)=><article key={node.code}><small>0{i+1} / {node.code}</small><h2>{pick(node.label,locale)}</h2><p>{pick(node.text,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.modules}>{d.images.slice(1).map((image,i)=><div key={image.id}><b>VISUAL_MODULE_{i+1}</b><ProjectPicture image={image} locale={locale}/></div>)}</section>:null}<section className={s.command}><code>{project.tools.join("  +  ")}</code>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">RUN {pick(l.label,locale)} ↗</a>)}</section><footer><Link href={`${d.base}${d.query}`}>← CLOSE MODULE</Link>{d.next?<Link href={d.projectUrl(d.next.slug)}>LOAD NEXT →</Link>:null}</footer></main>}
