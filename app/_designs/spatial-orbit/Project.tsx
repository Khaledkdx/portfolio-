import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function OrbitProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.space} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>← RETURN TO ORBIT</Link><span>OBJECT {String(d.index+1).padStart(2,"0")}</span></nav><header><div className={s.rings}/><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.planet}/>:null}</header><section className={s.satellites}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i} style={{"--i":i} as React.CSSProperties}><b>0{i+1}</b><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.orbits}>{d.images.slice(1).map((image,i)=><ProjectPicture key={image.id} image={image} locale={locale} className={i%2?s.far:s.near}/>)}</section>:null}<section className={s.dock}><p>{project.tools.join(" / ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">DOCK / {pick(l.label,locale)} ↗</a>)}</section></main>}
