import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function PitchProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);const slides=[{n:"02",label:content.labels.challenge,text:project.challenge},{n:"03",label:content.labels.solution,text:project.solution},{n:"04",label:content.labels.outcome,text:project.outcome}];return <main className={s.deck} dir={d.dir}><section className={s.title}><nav><Link href={`${d.base}${d.query}`}>[ CLOSE ]</Link><b>01 / 0{d.images.length+4}</b></nav><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.cover}/>:null}</section>{slides.map((slide,i)=><section className={`${s.slide} ${s[`c${i}`]}`} key={slide.n}><b>{slide.n}</b><h2>{pick(slide.label,locale)}</h2><p>{pick(slide.text,locale)}</p></section>)}{d.images.slice(1).map((image,i)=><section className={s.media} key={image.id}><b>0{i+5}</b><ProjectPicture image={image} locale={locale}/></section>)}<section className={s.end}><h2>{locale==="ar"?"جاهز للنمو؟":"READY TO GROW?"}</h2><p>{project.tools.join(" / ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}{d.next?<Link href={d.projectUrl(d.next.slug)}>NEXT DECK →</Link>:null}</section></main>}
