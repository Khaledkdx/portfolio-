import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function WarProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.monitor} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>[X] EXIT DOSSIER</Link><span>LIVE // CAMPAIGN_{d.index+1}</span></nav><header><div><i>MISSION BRIEF</i><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.feed}/>:null}</header><section className={s.console}><article><b>01 / {pick(content.labels.challenge,locale)}</b><p>{pick(project.challenge,locale)}</p></article><article><b>02 / {pick(content.labels.solution,locale)}</b><p>{pick(project.solution,locale)}</p></article><article><b>03 / {pick(content.labels.outcome,locale)}</b><p>{pick(project.outcome,locale)}</p></article><aside>{project.tools.map(t=><span key={t}>● {t}</span>)}</aside></section>{d.images.length>1?<section className={s.feeds}>{d.images.slice(1).map((image,i)=><div key={image.id}><label>CAM_FEED_{i+2}</label><ProjectPicture image={image} locale={locale}/></div>)}</section>:null}<section className={s.launch}>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">LAUNCH / {pick(l.label,locale)} ↗</a>)}</section><footer>{d.previous?<Link href={d.projectUrl(d.previous.slug)}>← PREV DOSSIER</Link>:<span/>}{d.next?<Link href={d.projectUrl(d.next.slug)}>NEXT DOSSIER →</Link>:null}</footer></main>}
