import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function FutureProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.signal} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>SYS.EXIT</Link><span>2040 / SIGNAL_0{d.index+1}</span></nav><header><div className={s.beacon}/><small>TRANSMISSION ACTIVE</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.scan}/>:null}</header><section className={s.timeline}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><i/><span>T+0{i+1}</span><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.telemetry}>{d.images.slice(1).map((image,i)=><div key={image.id}><code>VISUAL_PACKET::{i+1}</code><ProjectPicture image={image} locale={locale}/></div>)}</section>:null}<section className={s.terminal}><p>&gt; {project.tools.join(" :: ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">[OPEN] {pick(l.label,locale)}</a>)}</section></main>}
