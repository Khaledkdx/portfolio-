import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function ReelProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.reel} dir={d.dir}><section className={s.frame}><nav><Link href={`${d.base}${d.query}`}>KHALID / REEL</Link><span>00:{String(d.index+1).padStart(2,"0")}</span></nav><h1>{pick(project.title,locale)}</h1>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.visual}/>:null}<p>{pick(project.description,locale)}</p></section>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><section className={s.scene} key={i}><span>SCENE 0{i+1}</span><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></section>)}{d.images.slice(1).map((image,i)=><section className={s.imageScene} key={image.id}><b>TAKE {i+2}</b><ProjectPicture image={image} locale={locale}/></section>)}<section className={s.credits}><h2>BUILD. LAUNCH. GROW.</h2><p>{project.tools.join(" / ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}{d.next?<Link href={d.projectUrl(d.next.slug)}>NEXT REEL →</Link>:null}</section></main>}
