import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function CubesProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.city} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>← CUBE CITY</Link><b>BLOCK {d.index+1}</b></nav><header><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.cube}/>:null}</header><section className={s.blocks}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><b>MODULE_{i+1}</b><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.voxels}>{d.images.slice(1).map((image,i)=><ProjectPicture key={image.id} image={image} locale={locale} className={i%3===1?s.tall:""}/>)}</section>:null}<section className={s.build}>{project.tools.map(t=><span key={t}>{t}</span>)}{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">OPEN {pick(l.label,locale)} ↗</a>)}</section></main>}
