import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function CasebookProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.issue} dir={d.dir}><div className={s.mast}><Link href={`${d.base}${d.query}`}>THE GROWTH CASEBOOK</Link><span>ISSUE {d.index+1} · FIELD REPORT</span></div><header><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.summary,locale)}</p></div>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.lead}/>:null}</header><section className={s.copy}><aside>{project.tools.map(x=><span key={x}>{x}</span>)}</aside><article><p className={s.drop}>{pick(project.description,locale)}</p><h2>{pick(content.labels.challenge,locale)}</h2><p>{pick(project.challenge,locale)}</p><blockquote>{pick(project.solution,locale)}</blockquote><h2>{pick(content.labels.outcome,locale)}</h2><p>{pick(project.outcome,locale)}</p></article></section>{d.images.length>1?<section className={s.spread}>{d.images.slice(1).map(image=><ProjectPicture key={image.id} image={image} locale={locale}/>)}</section>:null}<section className={s.sources}><b>{locale==="ar"?"روابط العدد":"Issue sources"}</b>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}</section><footer>{d.previous?<Link href={d.projectUrl(d.previous.slug)}>← PREVIOUS STORY</Link>:<span/>}{d.next?<Link href={d.projectUrl(d.next.slug)}>NEXT STORY →</Link>:null}</footer></main>}
