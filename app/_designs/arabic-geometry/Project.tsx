import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function GeometryProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.folio} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>خالد / KHALID</Link><span>FOLIO ٠{d.index+1}</span></nav><header><div className={s.seal}>نمو</div><div><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></div></header>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.plate}/>:null}<section className={s.pages}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><span>﴿ ٠{i+1} ﴾</span><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.tiles}>{d.images.slice(1).map(image=><ProjectPicture key={image.id} image={image} locale={locale}/>)}</section>:null}<section className={s.colophon}>{project.tools.map(t=><span key={t}>{t}</span>)}{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}</section><footer><Link href={`${d.base}${d.query}`}>العودة / RETURN</Link>{d.next?<Link href={d.projectUrl(d.next.slug)}>الفصل التالي ←</Link>:null}</footer></main>}
