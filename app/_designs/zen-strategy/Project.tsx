import Link from "next/link";import {ProjectPicture} from "@/app/_components/ProjectPicture";import {pick} from "@/lib/site-content";import {projectDetailData,type ProjectDetailProps} from "../types";import s from "./project.module.css";
export default function ZenProject(props:ProjectDetailProps){const{content,locale,project}=props,d=projectDetailData(props);return <main className={s.paper} dir={d.dir}><nav><Link href={`${d.base}${d.query}`}>← index</Link><span>〇 {d.index+1}</span></nav><header><div className={s.ink}/><small>{pick(project.eyebrow,locale)}</small><h1>{pick(project.title,locale)}</h1><p>{pick(project.description,locale)}</p></header>{d.images[0]?<ProjectPicture image={d.images[0]} locale={locale} priority className={s.still}/>:null}<section className={s.thoughts}>{[[content.labels.challenge,project.challenge],[content.labels.solution,project.solution],[content.labels.outcome,project.outcome]].map(([l,t],i)=><article key={i}><span>{["一","二","三"][i]}</span><h2>{pick(l,locale)}</h2><p>{pick(t,locale)}</p></article>)}</section>{d.images.length>1?<section className={s.scroll}>{d.images.slice(1).map(image=><ProjectPicture key={image.id} image={image} locale={locale}/>)}</section>:null}<section className={s.end}><p>{project.tools.join("  ·  ")}</p>{d.links.map(l=><a key={l.id} href={l.url} target="_blank" rel="noreferrer">{pick(l.label,locale)} ↗</a>)}</section></main>}
